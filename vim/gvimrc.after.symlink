"" some things are only on mac
if has("gui_macvim")
  set macligatures
  ""set transparency
  set transparency=5
  ""set my font size to be legible and glorious ligatures
  set guifont=Fira\ Code:h16
endif

"" set guicolors
colors molokai
